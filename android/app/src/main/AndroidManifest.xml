<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ===== Permissions ===================================================
         INTERNET / NETWORK_STATE     → load the website + detect offline.
         CAMERA                       → so the website's QR scanner can use
                                        the camera (we forward the request
                                        from WebChromeClient.onPermissionRequest).
         RECORD_AUDIO                 → uncomment if your site uses voice input.
         ACCESS_FINE/COARSE_LOCATION  → so location-aware features (find
                                        nearest cafe, delivery address) work.
         READ_MEDIA_IMAGES (33+)      → file chooser for image uploads.
         READ_EXTERNAL_STORAGE (≤ 32) → same intent on older devices.
         POST_NOTIFICATIONS           → required since Android 13 if the
                                        WebView ever asks for it.
    ============================================================ -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- New-order alert plumbing. FOREGROUND_SERVICE / DATA_SYNC for the
         poller that runs while the owner is signed in. WAKE_LOCK keeps the
         CPU awake long enough to fire the alarm when the screen is off.
         USE_FULL_SCREEN_INTENT lets the high-priority notification launch
         OrderAlertActivity above the lock screen on Android 14+. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- Hardware features — none are required, so the install matrix
         covers Fire 7 (no camera, no GPS), Fire HD (camera, no GPS) and
         Play Store devices alike. Marking everything required=false is
         critical for Amazon Appstore where the device matrix is wider. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.camera.front" android:required="false" />
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.location.network" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.wifi" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <!-- Needed so the WebView can ask Android to launch wa.me, upi://,
         tel:, mailto: handlers without intent visibility being blocked
         on Android 11+ (package-visibility rules). Also lets us detect
         the Amazon Appstore + Play Store packages so StoreLauncher can
         deep-link straight into them. -->
    <queries>
        <!-- Store apps used for force-update deep links + installer detection. -->
        <package android:name="com.amazon.venezia" />
        <package android:name="com.android.vending" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tel" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="mailto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="upi" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="whatsapp" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="*/*" />
        </intent>
        <!-- Amazon Appstore deep-link scheme (amzn://apps/android?p=...). -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="amzn" />
        </intent>
        <!-- Google Play deep-link scheme (market://details?id=...). -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="market" />
        </intent>
    </queries>

    <application
        android:name=".WatShopApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.WatShopCafe"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false"
        tools:targetApi="34">

        <!-- Splash + config-fetch entry point. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.WatShopCafe.Splash"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".WebViewActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ErrorActivity"
            android:exported="false"
            android:theme="@style/Theme.WatShopCafe" />

        <activity
            android:name=".MaintenanceActivity"
            android:exported="false"
            android:theme="@style/Theme.WatShopCafe" />

        <activity
            android:name=".UpdateActivity"
            android:exported="false"
            android:theme="@style/Theme.WatShopCafe" />

        <!-- Full-screen new-order alert. Launched by the poller via the
             notification's fullScreenIntent. showWhenLocked + turnScreenOn
             so it lights the device above the lock screen. excludeFromRecents
             keeps it out of the recent-apps list (it's transient). -->
        <activity
            android:name=".orders.OrderAlertActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:showOnLockScreen="true"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@style/Theme.WatShopCafe" />

        <!-- Foreground service that polls the dashboard for new orders.
             foregroundServiceType=dataSync is required on Android 14+. -->
        <service
            android:name=".orders.NewOrderPoller"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- FileProvider for camera image capture from the WebView's file
             chooser. Required because we hand the camera a content:// URI
             on Android 7+. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
