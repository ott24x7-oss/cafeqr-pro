<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Camera capture buffers we hand to the WebView via FileProvider. -->
    <cache-path name="camera_cache" path="camera/" />
    <files-path name="files" path="." />
    <external-files-path name="ext_files" path="." />
</paths>
